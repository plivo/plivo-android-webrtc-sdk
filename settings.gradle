pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Plivo-webrtc-android-sdk"
include ':PlivoEndpoint'
include ':app'
project(':app').projectDir = new File(rootDir, 'plivo-android-quickstart-app/app')
